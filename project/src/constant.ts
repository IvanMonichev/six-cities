
enum AppRoute {
  Root = '/',
  Login = '/login',
  Favorites = '/favorites',
  Offer = '/offer'
}

enum PageModifier {
  Main = 'main',
  Login = 'login',
}

enum MainModifier {
  Index = 'index',
  Login = 'login',
  Favorites = 'favorites',
  Property = 'propery',
}

export {
  AppRoute,
  PageModifier,
  MainModifier
};
